{
  "mcpServers": {
    "adup": {
      "type": "http",
      "url": "${ADUP_STAGING_GATEWAY_BASE:-https://gateway-staging.adup.io}/mcp",
      "headers": {
        "Authorization": "Bearer ${ADUP_STAGING_API_KEY}"
      }
    }
  }
}
