{
  "mcpServers": {
    "adup": {
      "type": "http",
      "url": "${ADUP_GATEWAY_BASE:-https://gateway.adup.io}/mcp",
      "headers": {
        "Authorization": "Bearer ${ADUP_API_KEY}"
      }
    }
  }
}
