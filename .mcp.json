{
  "mcpServers": {
    "adup": {
      "type": "http",
      "url": "https://gateway.adup.io/mcp",
      "headers": {
        "Authorization": "Bearer ${user_config.ADUP_API_KEY}"
      }
    }
  }
}
